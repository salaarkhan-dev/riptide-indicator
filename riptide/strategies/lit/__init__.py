"""LIT — forward research only.

The HISTORICAL LIT trading family is CLOSED. Stages A, B and C each returned
INCONCLUSIVE under their own pre-registered criteria (PREREG_lit_stage_a.md,
_b, _c). Nothing in this package reopens that, re-slices it, or re-tunes it.

This package exists for ONE purpose: to record what the frozen Stage C
candidate does on setups that occur AFTER activation, and to score the
T6_PIVOT trail against the frozen control on the SAME setup.

It ships disabled. See PREREG_lit_forward_v1.md.
"""

from .types import FWD_VERSION, FWD_FAMILY, ForwardSetup, RULES  # noqa: F401
